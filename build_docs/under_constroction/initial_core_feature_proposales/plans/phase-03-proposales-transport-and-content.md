---
plan: 3
phase: Proposales adapter — transport, error translation, content read
state: PROMPT_READY
date: 2026-09-05
author: implementation-planner round 1
---

# Phase 3 — Proposales adapter: transport, error translation, content read

## Goal

Create `src/lib/proposales/` with the transport (`http.ts`: base URL, bearer auth, timeout, bounded retry on idempotent reads only), the total error translation and status-before-body precedence of §17A.13, the content read operations (`listContent`, `getContent`) and the company read (`getCompany`, owner card 1 → A) with wire schemas and the bounded epoch→ISO mapper, the recording fake's read half, and the integration README.

**Not in this phase:** proposal create, recovery search, get-proposal read-back, Applied Pricing (phase 4). No feature code.

## Read first

1. Master plan §5 (R10), §6.1, §6.3 (`ProposalesFailureReason`), §6.4 (`ContentItem`, `ProposalesClient` — declare all six, implement exactly the three reads now), §6.5 (`PROPOSALES_*`, upstream-error constants), §6.6 (`createFakeProposalesClient`), §9 rules 3, 5, §10.4.
2. Intention §17A.13 (Proposales table, what may cross), §17A.8 (retrieval paragraph), §17A.16 (time), §12.1, §10.1.
3. Evidence doc §1, §2, §3, §6 (timestamps), §7.
4. `api-documentation/proposales/openapi.json`: `GET /v3/content` parameters and `ContentItem`; `GET /v3/companies` and `Company` (`currency`, `tax_mode`); the error schema. Evidence §8.1 (the endpoint returned `currency` and `tax_mode` at runtime).
5. Contracts: `07-integrations.md` §1–§6, §10; `06-data-contracts-and-validation.md` §2, §3, §5, §6, §7; `04-server-architecture.md` §6; `10-security-and-trust-boundaries.md` §2, §4, §7, §8; `11-testing-principles.md` §2–§3 (integration client row); `14-documentation-principles.md` §9.

## Dependencies (gate)

Phase 2 `APPROVED`.

## Files expected to change

`src/lib/errors/app-error.ts`, `src/lib/errors/app-error.test.ts`, `src/lib/proposales/index.ts`, `http.ts`, `http.test.ts`, `errors.ts`, `errors.test.ts`, `schemas.ts`, `mappers.ts`, `mappers.test.ts`, `client.ts`, `client.test.ts`, `fake.ts`, `fake.test.ts`, `fixtures/content-list.json`, `fixtures/companies.json`, `fixtures/error-400-issues.json`, `README.md` — 18 files (16 new; the two shared-error files are amended).

## Implementation tasks (ordered)

1. `app-error.ts` / `app-error.test.ts`: extend `IntegrationErrorOptions` with `issues?: ErrorIssue[]` and preserve it as `details.issues`; prove the constructor shape. `errors.ts` (also `import "server-only"`): `ProposalesError extends IntegrationError` with `details.system = "proposales"`. `fromUpstream({ status?, bodyText?, parsedBody?, operation, kind: "http" | "transport" | "timeout" | "invalid_body" })` owns all transport/HTTP/body outcomes; `schemaMismatch(operation, zodError)` owns response-schema failures; `notFound(operation)` owns the configured-company-absent case. For HTTP outcomes, classify non-2xx status **before** body parsing (§17A.13): 429/5xx retain their retryable reason even with an unreadable body; `invalid_body` is only a 2xx unreadable body. Forward `error.message` only when it is a string within `MAX_UPSTREAM_MESSAGE_CHARS`, otherwise `GENERIC_UPSTREAM_ERROR_MESSAGE`. Map at most `MAX_UPSTREAM_ISSUES` issues in source order, `path.map(String)`, replacing an over-cap issue message with the generic message. Raw body, headers, and URL live only in `cause`, whose exact shape is `new Error(bodyText ?? GENERIC_UPSTREAM_ERROR_MESSAGE, { cause: { status, headers, url } })`; criteria inspect `cause.message`, never `String(cause)`.
2. `http.ts` (`import "server-only"`): `createProposalesHttp({ fetch = globalThis.fetch, apiKey, baseUrl = PROPOSALES_BASE_URL, timeoutMs = PROPOSALES_TIMEOUT_MS, now = performance.now, sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms)) })`; it is endpoint-agnostic and passes caller-provided `query` through verbatim. Build query strings with `URLSearchParams`. `get(path, query, { operation, idempotent: true })` alone retries retryable failures up to `PROPOSALES_READ_MAX_ATTEMPTS`, with backoff `PROPOSALES_READ_BACKOFF_MS × attempt` and an overall `PROPOSALES_READ_TOTAL_MS` deadline: each attempt's abort delay is `min(timeoutMs, remaining deadline)` and no retry starts after it. On attempts/deadline exhaustion, rethrow the last upstream `ProposalesError`. `post(path, body, { operation })` never enters that loop. Every request uses an `AbortController`, `Authorization: Bearer <apiKey>`, `Accept: application/json`, and JSON body encoding. The attempt timer remains authoritative through both response headers and body consumption: a stalled 2xx body is `timeout`; a stalled non-2xx body settles as its already-known status classification, preserving the status-before-body rule and bounded retry. Do not retain a post-race `timedOut` / `controller.signal.aborted` branch that is unreachable by the timeout-promise ordering; the body-race catch and normal status/body paths are the complete classification surface. Test `controller.signal.aborted` first after a fetch rejection (timeout); an un-aborted rejection is `transport`.
3. `schemas.ts` (`import "server-only"`): `contentItemResponseSchema` keeps only `product_id`, `variation_id`, `title`, optional `description`, bounded `created_at`, and optional `images`; `contentListResponseSchema` and singular `variationIdSchema` (`^[0-9]+$`). `created_at` is an integer whose `Date` ISO output is accepted by `isoTimestampSchema`; the response schema thereby rejects both invalid dates and extended-year ISO output before a mapper executes. Strip-by-default; no `.passthrough()`. Do not retain an unused error-body export: `errors.ts` owns its local defensive interpretation of the body. `companyListResponseSchema` keeps `{ id: int, currency: uppercase-then-validated currencyCodeSchema, tax_mode: z.enum(["standard", "simplified", "tax-free", "none"]) }`; any malformed currency or unknown enum is a response `schema_mismatch`.
4. `mappers.ts` (`import "server-only"`): `toContentItem(wire)` converts ids to strings and the already-bounded millisecond `created_at` to ISO with the evidence §6 assumption comment; write `description: wire.description === undefined ? {} : wire.description` with a comment. `toCompanyInfo(wire)` maps the already-normalized currency to `{ companyId, currency, taxMode }`.
5. `client.ts` (`import "server-only"`): declare all six `ProposalesClient` methods but return `Pick<ProposalesClient, "getCompany" | "listContent" | "getContent">` and implement exactly those three. The client, not transport, injects `company_id`: `listContent` queries exactly `{ company_id }`; `getContent` first parses one decimal `variationId` with `variationIdSchema`, throwing `ValidationError` on failure before a request, then queries `{ company_id, variation_id }` and re-verifies returned `variation_id` equality before mapping — a filter is a narrowing hint, never a proof; `getCompany` queries exactly `{}` and selects the configured id. Each response `safeParse` failure throws `ProposalesError.schemaMismatch`; absent configured company throws `ProposalesError.notFound`, with no status. All three are idempotent reads.
6. `fake.ts` (`import "server-only"`): phase 3 creates only `catalog`, optional `company`, `calls`, `writes = 0`, `assertNoWrites()`, and the three read methods. It records `{ op }` for no-argument methods and `{ op, input }` only for `getContent`; it creates no `proposals`, `editorOrigin`, `failNext`, or `stored` scaffolding (phase 4 owns that write half). `company` defaults to `{ companyId, currency: "EUR", taxMode: "standard" }`; `getCompany()` returns it and records `{ op: "getCompany" }`.
7. `index.ts` (`import "server-only"`): exports the interface type, the factories, and domain types. `getProposalesClient(deps = {})` lazily constructs the default with `serverEnv`, while allowing tests to override only `fetch`, `now`, and `sleep`; it is the sole default-configuration seam.
8. `README.md`: endpoints used (so far), configuration ownership, quirks (per-endpoint versions, `company_id` placement, ms timestamps, error body, keys added without version bump), error translation table, retry policy, the reserved metadata prefix `proposal_copilot_` (§17A.11; phase 4 fills the keys). Link the vendored reference; do not paraphrase it. Inline code uses ordinary Markdown backticks, never literal backslash-prefixed backticks.
9. Named mutations, revert, stamp, checkpoint commit.

## Acceptance criteria

All rows use an injected `fetch` double and injected `now`/`sleep`; the offline guard never fires.

| ID | Row | Fixture / setup | Exact expected outcome | Named mutation | Trace |
|---|---|---|---|---|---|
| C1(a) | fetch rejects | `fetch` throws `TypeError("ECONNREFUSED")` | `ProposalesError`: `details.reason === "transport"`, `retryable === true`, `status` undefined, `system === "proposales"`, `operation === "listContent"` | — | §17A.13, M6 (crit 9) |
| C1(b) | timeout | `fetch` never resolves; `timeoutMs = 50` | rejects with `reason === "timeout"`, `retryable === true`; the `AbortSignal` handed to `fetch` is aborted | — | §17A.13 |
| C1(c) | 400 with issues | fixture `error-400-issues.json` contains `MAX_UPSTREAM_ISSUES + 1` ordered issues, including `path: ["items", 0, "quantity"]` and one over-cap message | `reason === "bad_request"`, `retryable === false`, `status === 400`; `details.issues` is exactly the first `MAX_UPSTREAM_ISSUES` fixture issues in source order with every path segment stringified and the over-cap message replaced by `GENERIC_UPSTREAM_ERROR_MESSAGE` | MUT-03-7 `errors.ts` · `fromUpstream` · remove `path.map(String)` → C1(c) red | §17A.13 |
| C1(d) | 401 | body `{ error: { message: "Unauthorized" } }` | `unauthenticated_upstream`, false, 401 | — | §17A.13 |
| C1(e) | 403 | | `forbidden_upstream`, false | — | §17A.13 |
| C1(f) | 404 | | `not_found_upstream`, false | — | §17A.13 |
| C1(g) | 409 | | `conflict_upstream`, false | — | §17A.13 |
| C1(h) | 429 | | `rate_limited_upstream`, true | — | §17A.13 |
| C1(i) | 503 | | `server_error`, true | — | §17A.13 |
| C1(j) | 418 (other 4xx) | | `bad_request`, false | — | §17A.13 |
| C1(k) | non-JSON body | 200 with `<html>` | `invalid_body`, false | — | §17A.13 |
| C1(l) | JSON failing schema | a fresh 200 response per call with `{ data: "SCHEMA-BODY-SENTINEL" }` for `listContent` | `schema_mismatch`, false, `operation === "listContent"`, `details.issues` paths present, and `SCHEMA-BODY-SENTINEL` absent from `JSON.stringify(toErrorDto(err))` for that same schema-mismatch error | MUT-03-16 `client.ts` · `listContent` schema-failure path · construct the public `ProposalesError` with `JSON.stringify(body)` as its message → C1(l) red | §17A.13, 07 §3 |
| C1(m) | retryable status with unreadable body | 503 with `<html>` | `server_error`, `retryable === true`, `status === 503`; the read makes the configured retry attempts | MUT-03-8 `errors.ts` · `fromUpstream` · classify an unreadable body before its non-2xx status → C1(m) red | §17A.13 |
| C1(n) | integration-error issue support | `new IntegrationError({ system: "proposales", retryable: false, issues: [{ path: ["data", "0"], message: "bad" }] })` | `details.issues` exactly equals the supplied typed issue array | — | §17A.13, 04 §6 |
| C1(o) | non-2xx status check precedes body access | a Response-like 503 whose `ok` getter asserts `text()` has not yet been called, then whose readable `text()` returns an error body | `server_error`, `retryable === true`, `status === 503`; the read makes the configured retry attempts | MUT-03-11 `http.ts` · non-2xx branch · move response-body reading above the `!response.ok` guard → C1(o) red | §17A.13 |
| C2(a) | documented message crosses | 404 body `{ error: { message: "Company not found" } }` | `err.message === "Company not found"` | — | §17A.13 |
| C2(b) | over-cap message does not | message of `MAX_UPSTREAM_MESSAGE_CHARS + 1` chars | `err.message === GENERIC_UPSTREAM_ERROR_MESSAGE`; `err.cause` is an `Error` whose `.message` equals the original over-cap body | MUT-03-6 `errors.ts` · `fromUpstream` · drop the length check → C2(b) red | §17A.13 |
| C2(c) | non-string message | `{ error: { message: 123 } }` | generic message | — | §17A.13 |
| C2(d) | raw body never crosses | 502 body `"RAW-BODY-SENTINEL"` | `RAW-BODY-SENTINEL` absent from `err.message`, `JSON.stringify(err.details)`, and `JSON.stringify(toErrorDto(err))`; `err.cause` is an `Error` whose `.message` is exactly `RAW-BODY-SENTINEL` | MUT-03-1 `errors.ts` · `fromUpstream` · set `details.body = bodyText` → C2(d) red | §17A.13, M6 |
| C2(e) | URL never crosses | any failure | the request URL (contains `api.proposales.com`) absent from message and details | — | §17A.13, 10 §2 |
| C3(a) | read retried on retryable | `listContent`, fetch returns 503, 503, 200 | resolves; `fetch` called 3 times; `sleep` called with increasing delays | — | §17A.12 (read-back bounds), 07 §5 |
| C3(b) | read not retried on non-retryable | 401 | `fetch` called once | — | §17A.13 |
| C3(c) | attempts bounded | always 503 | rejects after exactly `PROPOSALES_READ_MAX_ATTEMPTS` calls with `server_error`, `retryable === true`, `status === 503` | — | §17A.12 |
| C3(d) | total elapsed cap | `now` advances past `PROPOSALES_READ_TOTAL_MS` after the first 503 | rejects after exactly 1 call with that last `server_error`, `retryable === true`, `status === 503`; no second attempt starts | — | §17A.12 |
| C3(e) | write never retried | `http.post("/v3/proposals", {}, …)` with 503 | rejects with `server_error`, `retryable === true`, `status === 503`; `fetch` called exactly once | MUT-03-2 `http.ts` · `post` · route through the retrying path → C3(e) red | §17A.11 (create never auto-retried), M5 |
| C3(f) | total deadline clamps an in-flight read | `fetch` never resolves; `timeoutMs = PROPOSALES_TIMEOUT_MS`; advance fake time to `PROPOSALES_READ_TOTAL_MS` | rejects `timeout` and the signal is aborted at the total deadline, before the longer per-attempt timeout | — | §17A.12, 07 §5 |
| C3(g) | 2xx body remains in the timeout envelope | fetch resolves 200 headers whose `text()` never settles; fake timers advance to the total read deadline | rejects `timeout` and the handed-out signal is aborted; the promise settles after headers instead of hanging | MUT-03-10 `http.ts` · request completion · clear the timeout immediately after `fetch` resolves → C3(g) red | §17A.12, §17A.13, 07 §5 |
| C3(h) | non-2xx stalled body retains status semantics | every GET fetch resolves a 503 whose `text()` never settles; fake timers advance each attempt timeout and `sleep` is immediate | rejects after exactly `PROPOSALES_READ_MAX_ATTEMPTS` calls with `server_error`, `retryable === true`, `status === 503`; every handed-out signal is aborted | MUT-03-10 `http.ts` · request completion · clear the timeout immediately after `fetch` resolves → C3(h) red; MUT-03-15 `http.ts` · non-2xx body-timeout catch · rethrow the timeout instead of reclassifying the known status → C3(h) red on `reason` and absent `status` | §17A.12, §17A.13, 07 §5 |
| C4(a) | listContent request shape | spy on `fetch` | URL path `/v3/content`; query keys exactly `["company_id"]` with the configured value; method `GET` | MUT-03-3 `client.ts` · `listContent` · add `include_archived=true` → C4(a) red | §17A.8 (company_id only; no filters) |
| C4(b) | auth header | | `Authorization` header present and starts with `Bearer `; the test asserts presence only | — | 10 §2 |
| C4(c) | getContent request shape | `getContent("188485")` | query keys exactly `["company_id","variation_id"]`, `variation_id === "188485"` | — | §17A.8 |
| C4(d) | getContent miss | empty list response | resolves `null` | — | §17A.8 |
| C4(e) | fake records reads | `createFakeProposalesClient({ catalog })` | `listContent()` returns the catalog; `calls` equals `[{ op: "listContent" }]`; `writes === 0`; `assertNoWrites()` does not throw | — | M3 (recording fake) |
| C4(f) | default factory wiring | `getProposalesClient({ fetch, now, sleep })` | its first list read sends `serverEnv.PROPOSALES_COMPANY_ID` as `company_id` and an `Authorization` header beginning `Bearer `; no real fetch is reached | — | 02 §3, 11 §3 |
| C4(g) | every adapter module is server-only | read the seven phase-3 `src/lib/proposales/*.ts` production files | each has `import "server-only";` as its first line | — | 02 §3 |
| C4(h) | variation id rejects untrusted shape | `getContent("188485/../../bad")` with a fetch spy | rejects `ValidationError`; fetch is called exactly zero times | — | 10 §8, 06 §2 |
| C4(i) | getContent re-verifies the requested identity | `getContent("222")` receives ordered wire items with `variation_id` 111 then 222 | resolves the mapped item whose `variationId === "222"`, never the first row by position | MUT-03-12 `client.ts` · `getContent` selection · replace equality selection with `data[0]` → C4(i) red | §17A.8, §17A.11 |
| C4(j) | getContent rejects an upstream filter mismatch | `getContent("222")` receives only a valid wire item with `variation_id` 111 | resolves `null` | MUT-03-12 `client.ts` · `getContent` selection · replace equality selection with `data[0]` → C4(j) red | §17A.8, §17A.11 |
| C4(k) | getContent accepts one variation id only | `getContent("111,222")` with a fetch spy | rejects `ValidationError`; fetch is called exactly zero times | MUT-03-13 `schemas.ts` · `variationIdSchema` · restore the comma-separated-list regex → C4(k) red | §10.2, §17A.8, 06 §2 |
| C5(a) | parse + map | `fixtures/content-list.json` (2 items, one with images) | `ContentItem[]` with `variationId`/`productId` strings, `title`/`description` records, `images` present only where given | — | §17A.8, §17A.16 |
| C5(b) | unknown keys stripped | fixture item carries `integration_metadata` and `future_key` | neither key present on the domain object | — | 06 §3 |
| C5(c) | epoch → ISO (ms assumed) | `created_at: 1757059200000` | `createdAt === "2025-09-05T08:00:00.000Z"`; test name states "assumes milliseconds" | MUT-03-5 `mappers.ts` · `toContentItem` · multiply `created_at` by 1000 → C5(c) red | §17A.16 |
| C5(d) | missing description | the second fixture item omits `description` | `description` deep-equals `{}` | — | §17A.8, 06 §6 |
| C5(e) | out-of-range epoch | a response item whose integer `created_at` is outside the four-digit ISO Date range | the whole `listContent` read rejects `ProposalesError` `schema_mismatch`, `retryable === false`, `operation === "listContent"`, with an issue path ending `created_at`; no partial catalogue returns | MUT-03-9 `schemas.ts` · `contentItemResponseSchema` · remove the Date/ISO range refinement → C5(e) red | §17A.16, §17A.13 |
| C5(f) | extended ISO year is rejected before mapping | a response item with `created_at: 253402300800000` (`+010000-01-01T00:00:00.000Z`) | the whole `listContent` read rejects `ProposalesError` `schema_mismatch`, `retryable === false`, `operation === "listContent"`, with an issue path ending `created_at` | MUT-03-14 `schemas.ts` · `boundedCreatedAtSchema` · remove only the `isoTimestampSchema` check → C5(f) red | §17A.16, §17A.13 |
| C6(a) | getCompany request shape | spy on `fetch` | path `/v3/companies`; method `GET`; query keys exactly `[]`; auth header present | — | §12.1 (card 1 → A), 07 §1 |
| C6(b) | selects the configured company | `fixtures/companies.json` has exactly two companies; configured id is the second, with `currency: "eur"`, `tax_mode: "standard"` | resolves exactly `{ companyId, currency: "EUR", taxMode: "standard" }` | MUT-03-4 `client.ts` · `getCompany` · take `data[0]` → C6(b) red | §12.1, 06 §6 (enum/id handling) |
| C6(c) | configured company absent | reuse `fixtures/companies.json` with a configured id absent from both entries | `ProposalesError` `not_found_upstream`, `retryable === false`, `operation === "getCompany"`, `status` undefined | — | §17A.13 |
| C6(d) | fake | `createFakeProposalesClient({ company })` | `getCompany()` returns `company`; `calls` gains `{ op: "getCompany" }`; `writes === 0` | — | rule 5 |
| C6(e) | malformed company currency | a company response has a three-character currency that fails the uppercase currency schema | `getCompany` rejects `ProposalesError` `schema_mismatch`, `retryable === false`, `operation === "getCompany"`; no bare `ZodError` escapes | — | §17A.13, 06 §6 |
| C6(f) | unknown company tax mode | a company response has `tax_mode: "future-mode"` | `getCompany` rejects `ProposalesError` `schema_mismatch`, `retryable === false`, `operation === "getCompany"` | — | 06 §6 |

Criteria: 6 (C1–C6), 51 rows (`C1` 15 + `C2` 5 + `C3` 8 + `C4` 11 + `C5` 6 + `C6` 6; a table line is one row). Named mutations: 16 (`C1` 4 + `C2` 2 + `C3` 3 + `C4` 3 + `C5` 3 + `C6` 1; MUT-03-10 covers both C3(g) and C3(h)).

## Notes

- `company_id` belongs to the endpoint-owning client: phase 3's content reads add it to their GET query and `getCompany` deliberately adds none. Phase 4's create client adds it to the POST body (evidence §4 `CreateProposalRequest.company_id`). The transport never knows an endpoint's company-id placement.
- The 12-condition table of §17A.13 collapses to 11 reasons because "any other 4xx" shares `bad_request`; C1 exercises both sources of `bad_request` and has one extra 503-with-HTML row proving the owner-ratified status-before-body precedence. That row adds no new reason.
- `fixtures/content-list.json` has exactly two items: the first has images, `integration_metadata`, `future_key`, and `created_at: 1757059200000`; the second omits `description`. `fixtures/companies.json` has exactly the two companies pinned in C6(b), and C6(c) changes only the configured id.
- `created_at` range validation belongs to the Proposales wire schema. Do not modify `src/lib/values/timestamp.ts`; its ISO-form contract remains phase 2's approved boundary.
- The vendor accepts comma-separated `variation_id` filters (evidence §3), but this application's `getContent` is the singular intention §10.2 operation and returns one `ContentItem | null`; its public validation therefore accepts exactly one decimal id. A future plural operation needs its own return type and criterion rather than silently borrowing this seam.
- Any test that invokes a client twice or exercises a retry supplies a fresh `Response` from `mockImplementation`; a `Response` body is single-use, and a shared fixture may otherwise make a later assertion observe an unrelated unreadable-body path.
- Do not implement pagination (none exists, evidence §1).
- Projection gate: mandatory (error precedence and taxonomy, rank 10).

## Review log

*(append-only)*

- **Projection round 0 consumed (2026-09-05, coordinator):** `AMENDMENTS_REQUIRED` ledger routed. Owner ratified card 1 → A in intention §17A.13 / §21.4 (status before unreadable response body) and card 2 → A in §17A.16 / §21.4 (out-of-range content epoch fails the full read as `schema_mismatch`). Plan folds D1–D21: shared `IntegrationError` issue support; explicit error constructors and cause shape; client-owned company injection; six-method/three-read interface; complete fixture perimeter; total-read deadline and exact retry outcomes; optional description, closed tax enum, bounded/mapped issue paths, schema-stage currency validation, factory seam, fake surface, all-module `server-only`, abort discriminator, registered defaults/generic message, and variation-id validation. Projection's five delegations remain explicit in its handoff.

- **Implementation round 1 (2026-09-05, Codex):** Implemented the transport, Proposales error taxonomy/translation, response schemas, mappers, three read client methods, default factory, read-only recording fake, fixtures, shared integration issue support, and integration README within the declared 18-file perimeter. The row-to-test map in the handoff discharges all 44 rows with 56 targeted tests before the C1(c) duplicate was removed; the final targeted phase run has 55 tests in 6 files. Judgment calls: the error body parser uses the phase schema shape as a defensive interpretation without throwing on malformed error bodies; a single `getContent` match is returned and multiple matches are defensively reduced to the first; the fake defaults its company id from `serverEnv`; the factory exposes only the three implemented reads while the six-method interface remains the phase registry seam. Status classification is kept in `fromUpstream` before body interpretation, and the total read deadline owns per-attempt timeout clamping. The initial details-option redaction probe was a false green because `IntegrationError` discards unsupported options; it was re-sited to mutate the actual details object and reddened C2(d). Additional guard probes reddened C4(g) when `fake.ts` lost its first-line directive and C4(h) when variation validation was bypassed; both were reverted. No architecture graph is present. Documentation impact review found the new integration README was required and no other authoritative document became false.

- **Post-checkpoint ordering audit (2026-09-05, Codex):** Moved the non-2xx branch ahead of response-body reading/parsing in `http.ts`, preserving the ratified status-before-body rule at the transport call site. Revalidated all 9 named mutations against the corrected tree; every mutation reddened its target assertion and was reverted. This correction is included in the follow-up checkpoint.

- **Independent review round 1 (2026-09-05, Claude Opus 5):** `CHANGES_REQUESTED`. Gate rows 1–4 pass; perimeter re-derived from `6722d19..5227b3f` is exactly 20 files (2 shared-error, 16 new Proposales, 2 documentation) and the follow-up checkpoint touches only `http.ts` plus this Review log. Review-entry L4 on HEAD `5227b3f` + dirty digest `e314424c` (differs from the implementer's stamp tree `10fee888`): `npm test` 11 files / 111 tests, ∅→∅ failure-ID delta. Trace chain closed: 44 criterion rows ↔ 44 phase-authored tests (http 22, client 14, mappers 4, fake 2, errors 1, app-error 1 of 12; the other 11 are inherited phase-2), **zero orphan tests**; 9/9 named mutations reconcile with the plan's declared arithmetic.

  **B1 (blocking) — a response body read is ungoverned by any timeout.** `http.ts:101` clears the attempt timeout the moment `fetch` resolves, so `readResponseBody`'s `await response.text()` at `:104` (non-2xx) and `:114` (2xx) runs with the abort controller disarmed. A `Response` whose `text()` never settles hangs the request forever: no timeout, no abort, no retry, no classification. Authority: `07-integrations.md` §5 ("Every request has a timeout. No exceptions."); plan task 2 ("Every request uses an `AbortController`"; attempt abort delay `min(timeoutMs, remaining deadline)`); §17A.12 read-back bounds; §17A.13 (the failure table is total over what the transport can produce — this path produces none); `02-runtime-boundaries.md` §9 (function duration limits). Also falsifies the README retry-policy paragraph. Reviewer probe (real timers, `timeoutMs: 50`, 1500 ms race): 503-with-stalled-body and 200-with-stalled-body both `STILL-PENDING` at `fetchCalls = 1`; control 503-with-readable-body rejects normally under the identical setup. This is the required fresh variation of C1(m), C1(k), C3(a), C3(f). **Correction:** keep the timeout armed until the body has been read — clear it after `readResponseBody`, or race `readResponseBody` against the same `timeoutPromise` — so the attempt timeout and the total read deadline govern headers *and* body.

  **B2 (blocking) — `getContent` returns an item the caller did not request.** `client.ts:42` takes `parsed.data.data[0]` with no equality check against the requested variation id. Reviewer probe: a two-item response to `getContent("222")` resolves `variationId: "111"`. Authority: §17A.8 ("the vendor's list order is never relied upon for anything"); §17A.11 sets the ratified precedent for exactly this shape ("Every returned row is re-verified in the client … the filter is treated as a narrowing hint and the equality is ours"), and evidence §3 documents the `variation_id` parameter without documenting its matching semantics. The implementer's recorded "defensively reduced to the first" judgment understates the outcome: the item returned can be a different product. Secondary: `variationIdSchema` admits comma-separated lists while the method returns a single `ContentItem | null`, so a multi-id query silently drops all but one. **Correction:** select by `String(item.variation_id) === parsedVariationId.data` and return `null` on no match; route the comma-separated-list question to the coordinator (§17A.8's `get_content` tool input is singular).

  **S3 (should-fix) — the transport-ordering correction ships unguarded.** Reviewer mutation: restoring `1d33f64`'s `http.ts` (body read before the `!response.ok` branch) leaves 55/55 phase tests green. The entire production change of checkpoint `5227b3f` has no test that bites on it. Charter rules 12 and 15. **Correction:** a transport-level row asserting ordering — e.g. a 503 whose `text()` is a spy, asserting classification does not depend on the body having been consulted.

  **S4 (should-fix) — the epoch refine has two sub-checks; one is uncovered.** Reviewer mutation: keeping the NaN guard and removing only the `isoTimestampSchema` check in `schemas.ts:14` leaves 55/55 green. C5(e)'s fixture `8640000000000001` exercises only the `Number.isNaN` branch; the extended-ISO family (`Date.UTC(10000,0,1)` = `253402300800000` → `+010000-01-01T00:00:00.000Z`) is rejected correctly in production but by no test. Charter rules 12 and 2. **Correction:** add a C5 row and a named mutation for the four-digit-ISO sub-check.

  **S5 (should-fix) — `errorBodySchema` is dead scaffolding.** `schemas.ts:35` exports it; it has zero importers repo-wide and no test caller. `errors.ts` defines its own inline `errorIssueSchema` instead. Charter rule 4. **Correction:** consume it in `fromUpstream`'s body interpretation or delete it. Plan lesson: task 3 mandated the symbol and task 1 independently re-derived its logic.

  **S6 (should-fix) — the integration README renders with literal backslashes.** 21 lines of `src/lib/proposales/README.md` contain `\`` (backslash + backtick), a heredoc artifact; every inline code span shows its backslashes and loses code formatting. Authority: master plan §9.0 ("anything a reader evaluating the build would see first … build quality is the deliverable"); `14-documentation-principles.md` §9. **Correction:** strip the backslashes. The same artifact is in the implementer handoff §3 (record only).

  **N7 (note) — asymmetric issue bounding.** `ProposalesError.schemaMismatch` (`errors.ts:138`) applies neither `MAX_UPSTREAM_ISSUES` nor `boundedMessage`, while `fromUpstream` applies both; a large malformed catalogue yields an unbounded public `details.issues`. Carry forward to phase 4.

  **N8 (note) — unbounded upstream work.** `mapIssues` safe-parses the whole issues array before slicing (verified: 30 invalid + 30 valid entries yields the first 25 *valid* ones in source order — defensible under "at most `MAX_UPSTREAM_ISSUES` in source order", but the cap counts valid entries only), and `response.text()` has no size bound. MVP-scoped; carry forward to phase 4.

  **N9 (note) — handoff §5 truncation.** The mutation-ledger paragraph ends mid-sentence ("It was not counted as executed evidence; the") followed by a stray `+`. Record only; a handoff is not rewritten.

  **N10 (note) — unreachable per-attempt timeout on reads.** `PROPOSALES_TIMEOUT_MS` (10 000) exceeds `PROPOSALES_READ_TOTAL_MS` (8 000), so `min()` on the GET path always yields the total deadline and the per-attempt constant governs only POST. These are the registry's stated values; flagged for visibility, no change requested.

  **Verified correct.** Perimeter and follow-up-checkpoint scope; L4 reproduction; trace chain with zero orphan tests; all 11 `ProposalesFailureReason` members matching the §6.3 registry and each §17A.13 status row exercised on the production path; status-before-body classification inside `fromUpstream` (`invalid_body` reachable only from a 2xx); redaction of raw body, headers and URL into `cause` alone with `toErrorDto` never serializing `cause` (`10-security` §2); hostile error-body shapes (non-array issues, `null` error, object message, array/string/null body) never throwing and always falling back to the generic message; unknown-key stripping at top, item and company-entry level with localized text rejecting non-string values; epoch boundary behaviour in production (9999-12-31 accepted, year 10000 rejected, negative years rejected, max-`Date` rejected, non-integer rejected); the fake's surface being exactly the plan's read half with `assertNoWrites` proven able to fail; client-owned `company_id` placement with an endpoint-agnostic transport and a query-free `getCompany`; `variationIdSchema` rejecting traversal shapes before any fetch and `URLSearchParams` query construction (`10-security` §8); `server-only` first in all seven adapter modules; writes never retried; retry counting, total-deadline clamping, abort discrimination and no-retry-after-deadline; and all constants matching the §6.5 registry.

  **Probe declaration.** Applied and reverted: `src/lib/proposales/http.ts` (restored from `1d33f64`, then restored), `src/lib/proposales/schemas.ts` (ISO sub-check removed, then restored). Created and deleted: `src/lib/proposales/zz-reviewer-probe.test.ts`. All 18 phase files checksum-verified byte-identical afterwards; `git status --porcelain` and the tracked dirty digest `e314424c` are unchanged from review entry. No database or other state side effect exists in this phase.

- **Coordinator review-round-1 fold (2026-09-05).** B1/B2 and S3–S6 are mandatory in fix round 2. The acceptance table now has 51 rows and 14 named mutations: C1(o)/MUT-03-11 proves the status condition is inspected before body access; C3(g–h)/MUT-03-10 separately prove that headers-plus-stalled-body settles as `timeout` for 2xx and as the retryable 503 status path for non-2xx; C4(i–j)/MUT-03-12 re-verify the requested variation id; C4(k)/MUT-03-13 narrows this singular client operation to one decimal id; and C5(f)/MUT-03-14 proves the extended-ISO sub-check. S5 is resolved by deleting the unused `errorBodySchema`; S6 by correcting the integration README's inline-code markup. The intentional client-side identity check is a direct application of the ratified §17A.11 filter-as-hint rule to §17A.8's singular get-content operation; it does not amend product semantics. N7/N8 remain routed to phase 4; N9/N10 are recorded only. The original 9 mutations are cited from checkpoint `5227b3f`; execute exactly MUT-03-10 through MUT-03-14 in this fix cycle.

- **Implementation fix round 2 (2026-09-05, Codex):** Resolved B1/B2 and S3–S6 within the six-file fix perimeter. `http.ts` keeps the attempt timer and timeout race active through response-body consumption; stalled 2xx bodies become `timeout`, while stalled non-2xx bodies retain their already-known status classification and retry behavior. `getContent` now re-verifies the requested variation id and the singular variation schema rejects comma-separated ids. Added C1(o), C3(g–h), C4(i–k), and C5(f) tests; deleted the unused `errorBodySchema`; stripped only literal backslashes before Markdown inline-code backticks in the integration README. The implementation follows the coordinator's prescribed corrections without divergence. The targeted phase suite is 6 files / 62 tests green; typecheck and lint are green. Closing L4 `npm test` ran exactly once on HEAD `5227b3f` plus dirty diff digest `0ef4f806391c188db6c5039e2a2b5b4f04f4c53693489ada289cde4bb35b7b1f`, with 11 files / 118 tests and failure-ID delta ∅→∅. Checkpoint `44e39e4` contains only the six final-perimeter files. Prior mutations MUT-03-1 through MUT-03-9 are cited from checkpoint `5227b3f`, not rerun; MUT-03-10 through MUT-03-14 were executed and reverted in this cycle, with all five reddening their named assertions. No owner decision card, architecture graph delta, or upstream semantic amendment arose; N7/N8 remain phase 4 work and N9/N10 remain records.

- **Independent re-review round 2 (2026-09-05, Claude Opus 5):** `CHANGES_REQUESTED`. Gate rows pass: intention `RATIFIED`; tracker row 2 `APPROVED`, row 3 `REVIEWING`; plan manifest re-derived independently as 6 criteria / 51 rows (C1 15 + C2 5 + C3 8 + C4 11 + C5 6 + C6 6) / 14 distinct named mutations, matching the plan's stated arithmetic. `git diff --name-status 5227b3f 44e39e4` is exactly the six declared files; nothing outside the perimeter changed and all pre-existing coordinator documents, handoffs, prompts and `tsconfig.tsbuildinfo` are untouched. Review-entry L4 (budget: exactly one, spent here because my tree differs from the implementer's stamp tree) on HEAD `44e39e4` + dirty digest `8b509a6a`: `npm test` 11 files / 118 tests, failure-ID delta ∅→∅ — reproducing the implementer's closing stamp on a different tree. `npm run typecheck` and `npm run lint` green at closeout. Handoff reconciled: 51 rows ↔ 51 phase-authored tests (http 25, client 18, mappers 4, fake 2, errors 1, app-error 1 of 12) inside a 62-test targeted suite, zero orphan tests; 14 = 9 cited `5227b3f` mutations + 5 newly executed (MUT-03-10…14); the nine prior mutations were correctly not rerun and I did not rerun them either.

  **B1 verified fixed.** `http.ts:101` snapshots `response.ok` before any body access; `:104` races `readResponseBody` against the *same* attempt `timeoutPromise`, and the handle is cleared only at `:130`, after the body race. A stalled 2xx settles as `timeout` with the handed-out signal aborted; a stalled non-2xx is reclassified at `:106–:114` from its already-known status, so a stalled 503 stays `server_error` / retryable and consumes exactly `PROPOSALES_READ_MAX_ATTEMPTS`. Fresh variation (a body-failure shape the fix ledger does not use — `text()` **rejecting asynchronously** after headers, rather than stalling): 2xx → `invalid_body`, `retryable === false`, one attempt; 503 → `server_error`, `retryable === true`, 3 attempts. Second fresh variation on a status the ledger does not use: a stalled **429** keeps `rate_limited_upstream` / retryable across 3 bounded attempts. No raw body, headers or URL crosses either fallback — both call `fromUpstream` without `bodyText`/`parsedBody`, so `message` is the generic constant and `cause` carries only `{status, headers, url}`. Timeout cleanup cannot produce a second classification: `requestOnce` throws exactly once on every path, and a deliberately *late-resolving* 2xx body (resolving 60 ms after its 40 ms attempt timer) settled the caller exactly once as `timeout` with zero unhandled rejections.

  **B2 verified fixed.** `client.ts:42` selects by `String(entry.variation_id) === parsedVariationId.data` before `toContentItem`, and returns `null` on no match. Fresh variations different from the recorded `111,222` ordering: requested id **last** in a reversed vendor list (`[222, 111]`, `getContent("111")`) resolves the id-111 row; **duplicate** matches (`[222, 111, 111]`) resolve a matching row and never the decoy. The result is not determined by vendor position.

  **S3 verified fixed.** C1(o) is a real ordering guard: its `ok` getter asserts `text()` has not been called, and the implementer's MUT-03-11 reddened it. Independently, a *different* mutant shape than any declared one — deleting only the non-2xx reclassification at `:106–:114` — reddened C3(h) on its exact sub-check (`reason` observed `timeout`, expected `server_error`; `status` absent), proving the status-retention half of B1 is load-bearing and not merely reached by a hang.

  **S4 verified fixed.** The four-digit-ISO boundary is exercised through the production `listContent` schema path. I enumerated the adjacent pairs at both edges rather than the single row: `253402300799999` (`9999-12-31T23:59:59.999Z`) maps successfully; `253402300800000` rejects `schema_mismatch`; `-62167219200000` (`0000-01-01T00:00:00.000Z`) maps successfully; `-62167219200001` rejects. Invalid-date protection remains in force (`8640000000000001` rejects).

  **S5 verified fixed.** `errorBodySchema` is gone; no importer exists (typecheck green) and no duplicate parser was introduced — `errors.ts` is outside the fix perimeter and still owns the single local `errorIssueSchema`.

  **S6 verified fixed.** Zero literal backslash-backticks remain in `src/lib/proposales/README.md`; every inline span is ordinary Markdown. The retry paragraph is now true of the code again (attempts bounded by the configured timeout or the remaining read deadline, body included), and the error-translation table still matches the eleven `ProposalesFailureReason` members.

  **C4(k) verified.** A comma-separated value never reaches `fetch`: `variationIdSchema` is `^[0-9]+$` and `getContent` validates before the request. Fresh variation over ten near-miss shapes (`"1,2"`, `" 111"`, `"111 "`, `"+111"`, `"1e3"`, `"111,"`, `",111"`, `"11 1"`, `"0x1f"`, `""`) — all reject `ValidationError` with `fetch` called zero times, while a valid `"7"` reaches `fetch` with `variation_id=7` verbatim. The public operation still returns `ContentItem | null`; no plural shape was reintroduced.

  **S7 (should-fix) — the B1 fix ships twelve lines of unreachable production code with no test that can distinguish their presence.** `http.ts:118–129` (`if (timedOut || controller.signal.aborted) { … }`) can never execute. `controller` is aborted only by this request's own timer; that timer callback rejects `timeoutPromise` synchronously, and microtasks drain fully before the next timer macrotask, so `readResponseBody` cannot win the `:104` race after `timedOut` is set. Reviewer mutation (deleting the whole block): `src/lib/proposales/` + `src/lib/errors/` stay green — 8 files / 77 tests, including a probe file written specifically to land a body result late. Authority: charter rules 4 (no dead scaffolding), 12 and 15 (a guard ships with proof that it can fail); `11-testing-principles.md` §5 ("a bug fix comes with a test at the lowest layer that could have caught it"). This is a recurrence, inside the S3 fix, of the exact family S3 named. **Correction:** delete the block — the two classifications it produces are already produced by the `:105` catch and the normal `:132`/`:142` path — or, if it is retained as defence in depth, add the criterion row and named mutation that make it fire.

  **S8 (should-fix) — C1(l)'s redaction sub-check cannot fail.** `client.test.ts:23–42` uses `mockResolvedValue(response({ unexpected: 1 }))`, one single-use `Response` for both calls. The first `listContent()` consumes the body and yields `schema_mismatch` (the row's first half, correct). The second call — the one the redaction assertion inspects — hits `Body is unusable`, so `readResponseBody` returns `{}` and the error is **`invalid_body`**, whose body was never read at all. Verified by probe: `second.details.reason === "invalid_body"`. The row requires body text absent from `JSON.stringify(toErrorDto(err))` *for the schema-mismatch error*; as written it asserts that of a different error, and would hold no matter what `schemaMismatch` serialized. Authority: charter rule 2 companion (a row's fixture must make its own predicate the only reason the outcome holds), rule 15, `11-testing-principles.md` §5. **Correction:** give each call its own `Response` (a `mockImplementation` factory), and choose a fixture whose offending text a naive implementation would actually surface — `{ "unexpected": 1 }` yields a zod issue of path `["data"]` and never contains the string being searched for, so the assertion stays weak even once the error is the right one.

  **N11 (note, plan lesson) — MUT-03-10 reddens C3(h) by hanging, so it never reaches C3(h)'s own assertions.** Under "clear the timeout immediately after fetch resolves" both C3(g) and C3(h) fail by Vitest test timeout; no assertion in either test executes. The declared ledger is arithmetically closed and each mutation reddens its named row, but charter rule 12 ("a named mutation must be shown to reach every sub-check, not merely to redden the test") is not satisfied for C3(h)'s `server_error`/`status: 503` sub-check by any *declared* mutation. I verified the sub-check is sound with an undeclared mutant (see S3 above); the gap is in the plan's mutation set, not in the code.

  **N12 (note) — single-use `Response` objects are shared across retry attempts in several settled rows.** C1(m) and C3(c) use `mockResolvedValue` with one `Response`; attempts 2 and 3 therefore read a body that is already consumed, so the error finally reported comes from a `Body is unusable` attempt rather than from the row's `<html>` / JSON fixture (verified: the final `cause.message` is the generic constant, not `<html>`). The rows still measure their predicate — "unreadable body, status retained" — via a different unreadability cause, and attempt 1 does exercise the stated fixture, so no behaviour is wrong. Fixture hygiene only; fold into the plan or carry to phase 4.

  **Verified correct (delta surfaces).** Six-file perimeter and its exact contents; the production diff is three files and nothing in `errors.ts`, `mappers.ts`, `fake.ts`, `index.ts` or the fixtures moved; L4 reproduction of the implementer's stamp on a different tree; row/test/mutation reconciliation with zero orphan tests; `responseIsOk` captured before body access and reused at `:132` so the ordering cannot drift; the body race preserving both bounded GET attempts and the `min(timeoutMs, remaining)` clamp; POST still single-attempt; abort discrimination unchanged; `variationIdSchema` narrowing without touching the traversal guard C4(h) already owns; `boundedCreatedAtSchema` retaining both sub-checks; README inline code, error table and retry paragraph all true of the shipped code. `vi.useFakeTimers()` in this project also fakes `performance.now()` (verified), so C3(f–h) are deterministic single- or fixed-attempt tests rather than real-clock races.

  **Probe declaration.** Applied and reverted in `src/lib/proposales/http.ts`: (1) removal of the `:106–:114` non-2xx reclassification; (2) removal of the `:118–129` `timedOut` block. Both reverted with `git checkout --`; final `shasum -a 256` is `d6bcdf56…` , byte-identical to review entry. Created and deleted: `src/lib/proposales/zz-reviewer-r2-probe.test.ts`, `src/lib/proposales/zz-reviewer-r2-probe2.test.ts`. All thirteen `src/lib/proposales` files checksum-verified byte-identical afterwards; `git status --porcelain`, dirty digest `8b509a6a…` and the untracked-file digest `b92e5f60…` are unchanged from review entry. No database or other state side effect exists in this phase. No production code was fixed by this session.

- **Coordinator re-review-round-2 fold (2026-09-05).** S7/S8 are mandatory in fix round 3: delete the timeout post-race branch proven unreachable by the Promise-race scheduling, and make C1(l)'s second call receive a fresh sentinel-bearing schema-mismatch response so the DTO redaction assertion evaluates the correct error. N11 is folded, not carried: C3(h) gains MUT-03-15, which rethrows the body-timeout instead of status reclassification and must redden its exact `reason` / `status` assertions. N12 is folded as test-fixture hygiene: C1(m) and C3(c) use a fresh Response per retry. C1(l) gains MUT-03-16, a temporary `client.ts` schema-failure-path mutation that puts `JSON.stringify(body)` in the public error message and must redden the sentinel redaction assertion. The table remains 51 rows; named mutations are now 16. N7/N8 remain phase-4 work; N9/N10 remain records. The fix round cites MUT-03-1 through MUT-03-14 from their checkpoints and executes exactly MUT-03-15 and MUT-03-16.

- **Implementation fix round 3 (2026-09-05, Codex):** Resolved S7/S8 and folded N11/N12 within the three-file final code/test perimeter. Deleted the unreachable post-race `timedOut || controller.signal.aborted` branch from `http.ts`; the settled body race, status-before-body handling, retry bounds, and error redaction remain unchanged. Changed C1(m) and C3(c) to return fresh `Response` instances from `mockImplementation` factories. Changed C1(l) to use a fresh `{ data: "SCHEMA-BODY-SENTINEL" }` response for each call, assert that the second error is `schema_mismatch`, and assert the sentinel is absent from `JSON.stringify(toErrorDto(error))`. The prescribed corrections were implemented without divergence. The pre-edit targeted baseline was 6 files / 62 tests, failure-ID delta ∅→∅; post-fix targeted evidence is 6 files / 62 tests, failure-ID delta ∅→∅. `npm run typecheck` and `npm run lint` passed. MUT-03-15 and MUT-03-16 were applied at L1, each reddened its named assertion, and each was reverted; the 14 prior mutations are cited from their existing checkpoints and were not rerun. No owner decision card, architecture graph delta, upstream semantic amendment, N7/N8 change, or N9/N10 rewrite arose. Documentation-impact review found no durable current-state documentation false or incomplete; only this Review log and tracker row 3 were required closeout records.

- **Implementation fix round 3 closeout (2026-09-05, Codex):** The single closing L4 `npm test`
  run passed with 11 files / 118 tests and failure-ID delta ∅→∅ at HEAD `44e39e4b…` plus dirty
  tracked-diff digest `34bb5a6e…`. The targeted 62-test suite, typecheck, lint, and diff check
  are green. Tracker row 3 is `IMPLEMENTED`; checkpoint commit contains only the three final
  perimeter files.

- **Coordinator approval (2026-09-05).** The owner explicitly declined a further independent
  round-3 re-review as disproportionate. Coordinator validation confirmed checkpoint `5fd0e61`
  changes exactly `http.ts`, `http.test.ts`, and `client.test.ts`; the S7 branch is deleted;
  C1(l) uses a fresh sentinel-bearing response for each call and asserts the second error is
  `schema_mismatch`; C1(m)/C3(c) use fresh response factories; MUT-03-15/16 evidence reaches the
  promised assertions; and the 51-row / 62-test / 16-mutation ledger reconciles. The phase is
  `APPROVED` with one explicit caveat: the round-3 delta has coordinator validation but no separate
  independent re-review. N7/N8 remain phase-4 work; N9/N10 remain records.
